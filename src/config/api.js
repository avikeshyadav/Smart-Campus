
const protocol = window.location.protocol;
const hostname = window.location.hostname;
const port = window.location.port;
export const BASE_URI = `${protocol}//${hostname}:${port}`;
// export const BASE_URI ="https://169.254.39.6:5173";
